import { ListaRegistrosScreen } from '@/components/lista-registros-screen';

export default function ListaLoginsScreen() {
  return <ListaRegistrosScreen variant="logins" title={'Lista de\nLogins'} />;
}
